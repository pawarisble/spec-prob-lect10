<h1>Editing item</h1>

<%= render "form", item: @item %>

<br>

<div>
  <%= link_to "Show this item", @item %> |

  <% if(session[:from_user_item] == true) %>
    <%= link_to "Back to items", main_user_Item_path  %>
  <% else %>
    <%= link_to "Back to items", items_path %>
  <% end %>
  
</div>